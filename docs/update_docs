#!/bin/bash

# Checking out and unstaging required files from master
git checkout master ../include source config Makefile
git rm -r --cached ../include source config Makefile

# Building docs
make html

# Cleaning up
rm -r ../include source config Makefile
